# ShareFare

A full-stack bill-splitting application built with Next.js, TypeScript, Firebase, Tailwind CSS, and Framer Motion.

## Status

The hosted demo is no longer active. This repository remains available as a code sample.

## Local development

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.
